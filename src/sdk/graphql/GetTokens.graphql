query GetTokens($input: [TokenInput!]) {
  tokens(ids: $input) {
    address
    decimals
    id
    name
    networkId
    symbol
    totalSupply
    createBlockNumber
    createdAt
    createTransactionHash
  }
}
